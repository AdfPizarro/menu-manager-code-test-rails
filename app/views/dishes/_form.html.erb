<%= form_with(model: dish) do |form| %>
  <% if dish.errors.any? %>
    <div style="color: red">
      <h3><%= pluralize(dish.errors.count, "error") %> prohibited this dish from being saved:</h3>

      <ul>
        <% dish.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  

  <div>

  <div>
    <%= form.label :menu_id, style: "display: block" %>
    <%= form.collection_select :menu_id, @menus, :id, :name %>
    <%=form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  
    <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>


  <div>
    <%= form.submit class: "button" %>
  </div>
<% end %>
